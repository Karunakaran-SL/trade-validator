package com.coding.trade.validator;

import org.springframework.validation.Errors;

public class SpotValidator extends BaseValidator {

    @Override
    public void validate(Object o, Errors errors) {
        super.validate(o, errors);
    }
}
